import CoreGraphics

/// Layout constants for the popover (design preview measurements).
enum CFSpacing {
    /// Fixed popover width (FR-U2).
    static let popoverWidth: CGFloat = 380
    /// Horizontal padding inside a popover section.
    static let sectionH: CGFloat = 20
    /// Vertical padding inside a popover section.
    static let sectionV: CGFloat = 18
}
